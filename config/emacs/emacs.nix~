{ lib, pkgs, ... }:

{
    programs.emacs.extraPackages = epkgs: [
        epkgs.rust-mode
        epkgs.nix-mode
        epkgs.eglot
        epkgs.nixos-options
    ];

    home.file.".emacs.d" = {
        source = ./emacs;
        recursive = true;
    };
}
